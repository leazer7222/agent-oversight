# Session 2026-06-10

## Focus
PostHog analytics instrumentation audit + integration planning for the ReformAI product
(`ReformAI-Inc/Reform-AI` @ `d768f37`, analyzed via the read-only clone at `.workspace/Reform-AI`).
No changes were made to the product repo — all deliverables are planning docs in this repo's `docs/`.

## Completed
1. **Recovered from a hard tool-deadlock.** An early Bash call did `cd .workspace/Reform-AI`,
   and the Bash tool's CWD persists across calls. The project's PreToolUse hooks use RELATIVE
   paths (`python scripts/hooks/...`), so they resolved into the clone, failed to find the
   script, and python's "file not found" exit code 2 was read by Claude Code as "block tool."
   The `.*` matcher blocked EVERY tool — including the `cd` to undo it. Cleared by a session
   restart (resets Bash CWD).
   - **Permanent fix applied:** all three hook commands in `.claude/settings.json` now use
     `$CLAUDE_PROJECT_DIR/...` absolute paths. Lesson appended to `LESSONS_LEARNED.md`.
2. **Instrumentation audit** -> `docs/reformai-posthog-instrumentation-audit.md`.
   ~110 named events across Platform/Auth, Homeowner, Service Provider, Seller, plus dedicated
   sections for the two hypothesis-critical features (AI Visualizer, Style Tournament). P0/P1/P2
   tiers, snake_case noun_verb names, status-enum reference, and a 12-item Instrumentation Gaps list.
   Key finding: ZERO analytics exist today; the Style Tournament is 100% client-side; Supplier
   persona + "Signature Package" upgrade do not exist yet.
3. **Preliminary integration analysis** (in-conversation) — decisions (region, reverse-proxy,
   identified_only, consent), then a structural readiness scan of the real wiring.
4. **Production implementation plan** -> `docs/reformai-posthog-implementation-plan.md`.
   Engineer-ready: locked decisions D1-D8, Phase 0 (9 tasks w/ real diffs + acceptance), Phase 1
   (P0 events), Phase 2/3 outlines, rollout/risk, and a PR file-change inventory.
   - **Critical correction caught from source:** `distinct_id` must be the **Firebase UID**
     (`user.uid` client / `req.user.firebaseId` server), NOT the DB UUID — the client never holds
     the DB row. DB UUID becomes a person property.
   - **Gap caught:** no `pnpm-workspace.yaml` exists; shared `@reformai/analytics` package needs
     it created (Task 1.0).

## Deliverables (this repo)
- `docs/reformai-posthog-instrumentation-audit.md`
- `docs/reformai-posthog-implementation-plan.md`
- `.claude/settings.json` (hook paths hardened)
- `LESSONS_LEARNED.md` (new lesson: relative hook paths + shell CWD = deadlock)

## Next steps (if resumed)
- Option A: generate branch/PR-ready diffs against the product repo (clone here is read-only).
- Option B: expand Phase 2 full visualizer/style-game event code.
- Option C: draft PostHog dashboard/funnel definitions (insight JSON).

## Notes
- The clone at `.workspace/Reform-AI` is read-only analysis space. Never `cd` into it from a Bash
  call (CWD persists and breaks relative-path hooks). Use absolute paths / Glob-Grep-Read / Explore
  subagents instead.
