# Session 2026-06-02

## Starting context
- Thread opened for the ReformAI agile-team-orchestrator.
- Existing: agile orchestrator (`b2c3d4e5-...`) runs a single worker, the Product
  Clarification Agent (PCA). Phase 1 only.
- Today's goal: bring `reformai.codebase-context-agent` (CCA) and `reformai.ba-scoping-agent`
  (BA) - both registered-but-runtime-pending, created 2026-06-02 - under the orchestrator,
  then expand the orchestrator into a full product-delivery lifecycle coordinator.

## What was done (design + documentation only - NO code/migrations applied)

### 1. Integration analysis (CCA + BA)
- Confirmed CCA and BA are a paired system split by direction of truth:
  CCA = IS-state (reads source, owns `cbc:*`), BA = SHOULD-BE (owns `CON-*`, never reads code).
- Hard dependency: `CCA -> (codebase-context.json, schema-validated) -> BA`. They are an
  ordered 2-stage unit, never independently schedulable.
- Both are docs-only shells: no `agent.py`, migrations 024/025 authored-not-applied,
  instances `status=paused`.

### 2. Pipeline-shape decision
- Recommended and confirmed: full chain `PCA -> CCA -> BA`, PCA's `restated_goal` becomes
  the `feature_intent` threaded into CCA + BA. PCA/CCA/BA operate at different altitudes
  (intent / code reality / scope reconciliation) - complementary, not competing.

### 3. Full lifecycle design (Agent Agile Force)
- Reframed the orchestrator from a scoping pipeline into a **lifecycle coordinator**
  (idea -> production).
- Wrote `docs/agent-agile-force-lifecycle.md` defining: full FSM states, stage ownership,
  per-stage artifacts, human gates (A-D), durable state vs immutable artifacts, now-vs-deferred,
  the BA->UX->Eng->CodeReview handoff transforms, deployment gating, future agents, phasing,
  and the no-rewrite stage-descriptor engine.
- Locked decisions: dedicated UX Design Agent (existing marketing `ui-design-agent` stays
  separate/out-of-lifecycle); design and implementation are distinct stages; lifecycle spine
  built now in Phase 2; `lifecycle_events` append-only; artifacts immutable; spine stores
  state + references only.

### 4. Lifecycle spine migration (026 - authored, NOT applied)
- Wrote `supabase/migrations/026_feature_lifecycle.sql` following the 025 platform pattern:
  - `platform.feature_lifecycle` (hybrid; frozen feature_id/tenant_id/created_at via trigger;
    `current_state` CHECK enumerates the full state set; `artifact_pointers` jsonb = references only)
  - `platform.lifecycle_events` (append-only via `apply_append_only_rls`)
  - `platform.gate_decisions` (append-only; gate CHECK `A|B|C|D`)
  - 4 public SECURITY DEFINER RPCs: `lifecycle_create_feature`, `lifecycle_transition`,
    `lifecycle_record_gate`, `lifecycle_get`
  - 1 freeze trigger, schema_registry governance rows (RFC-AAF-001)
- Linter clean: `python scripts/check_migrations.py --from-migration 026` -> 0 hard failures.

### 5. Added two future stages (design only)
- **Persona Validation Agent** (Phase 2.5, optional): after PCA, before CCA. New state
  `persona_validating`. Output `persona_validation_report` (desirability/frequency/pain/
  strategic value/objections + proceed signal). Advisory, skippable.
- **Sprint Planning Team** (Phase 3): after Gate A, before UX Design. New state
  `sprint_planning`. Team/sub-orchestrator. Output `sprint_planning_brief`
  (epics/stories/deps/risk/sequencing/sprint fit). Informs UX Design.
- FSM grew 17 -> 19 states. Roadmap renumbered (Sprint Planning = P3; UX/Eng/Review/Release
  shifted +1 -> P4/P5/P6/P7).
- **026 amended** (required, not optional): the two new state values added to the
  `current_state` CHECK enumeration - a closed list, so a transition into an un-enumerated
  state would violate the constraint. Zero-cost because 026 is not yet applied. No other
  026 change. Linter re-run: clean.

## Files changed this session
- `docs/agent-agile-force-lifecycle.md` (NEW - full lifecycle design spec)
- `supabase/migrations/026_feature_lifecycle.sql` (NEW - authored, NOT applied)
- `AGENTS.md` (agile orchestrator reframed as lifecycle coordinator + roadmap + planned agents)
- `sessions/2026-06-02.md` (this file)
- `LESSONS_LEARNED.md` (new lessons appended)

## Status
DESIGN COMPLETE for the lifecycle. Nothing applied to the DB; no agents unpaused;
no `agent.py` written. Current implementation scope remains PCA -> CCA -> BA -> Gate A
(Phase 2), unbuilt.

## Next steps (Phase 2 build, when ready)
1. Apply migrations in order: 024 (product_graph) -> 025 (cbc registry) -> 026 (lifecycle spine),
   each gated behind `check_migrations.py`.
2. Write `agent.py` for CCA first (BA's fixtures depend on a real codebase-context.json), then BA.
3. Refactor `agents/teams/agile/run.py` from single-worker into the generic stage-descriptor
   FSM engine; move PCA-specific doc-bundle/48h-staleness logic into the PCA stage input_adapter.
4. Wire `parent_run_id` so the lifecycle renders as one tree on the hierarchy page.
5. Implement Gate A via `public.lifecycle_record_gate`.
6. Set env (`CODEBASE_CONTEXT_AGENT_ID`, `BA_SCOPING_AGENT_ID`, `GITHUB_CODEBASE_AGENT_TOKEN`),
   unpause the CCA/BA instances.
