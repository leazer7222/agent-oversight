# Session 2026-05-18

## Starting context
- Last session (2026-05-15): fixed quota sync, dashboard timestamps, recommendation engine, duplicate provider_accounts
- Today: Phase 1 Group 1 of the Adaptive Cost Risk Engine
- Pre-build (migration 012) is already applied to Supabase project hdhovyrlnfojtkqbcegh
- RFCs (001-010) are in the funny-heisenberg worktree (untracked files, not yet merged to main)

## What we're building
Phase 1 Group 1 — three tables with no upstream dependencies:
1. `cost_intelligence.pricing_table_versions` + seed (10 models: Anthropic, OpenAI, Google)
2. `cost_intelligence.task_taxonomy_versions` + `cost_intelligence.task_types` + seed (8 types)
3. `telemetry.raw_events` + `/api/telemetry/ingest` endpoint

## Files created this session
- `scripts/check_migrations.py` — RFC-009 linter (copied from funny-heisenberg worktree)
- `supabase/migrations/013_cost_risk_engine_phase1_group1.sql` — pricing + taxonomy tables + seeds
- `supabase/migrations/014_cost_risk_engine_phase1_telemetry.sql` — raw_events table
- `src/app/api/telemetry/ingest/route.ts` — RFC-003 compliant ingest endpoint

## Key decisions
- New ingest endpoint at `/api/telemetry/ingest` (not `/api/ingest`) to avoid conflict with existing agent oversight endpoint
- `raw_events` has both `created_at` (RFC-009 linter requirement) and `received_at` (RFC-003 semantic name); both default to `now()`
- Task type seeds use a DO block with a local variable for taxonomy_id — avoids hardcoded seed UUIDs
- Pricing seed covers: claude-opus-4-7, claude-sonnet-4-6, claude-haiku-4-5-20251001, gpt-4o, gpt-4o-mini, o1, o3-mini, gemini-2.0-flash, gemini-2.0-flash-exp, gemini-1.5-pro
- Auth for telemetry ingest: same `x-agent-secret` / `INGEST_SECRET` pattern as existing endpoint
- Deduplication: insert + catch Postgres error code 23505 (unique_violation), return 200 with duplicate:true

## Phase 1 Group 2 (also completed this session)
- `supabase/migrations/015_cost_risk_engine_phase1_group2.sql` — ALTERs `runs` table:
  - Adds task_type_id UUID NOT NULL FK → cost_intelligence.task_types(id)
  - Adds task_complexity_bucket TEXT NOT NULL CHECK ('simple','medium','complex')
  - Adds task_classifier_version TEXT NOT NULL
  - Adds secondary_task_type_id UUID (nullable)
  - Backfills historical rows as code_gen/medium/complexity-v1
  - Two monitoring indexes (per RFC-005 §9)
- Updated `src/app/api/ingest/route.ts`:
  - New optional fields: task_type_code (string), task_complexity_bucket (enum)
  - On run_started: looks up task_type UUID from cost_intelligence.task_types
  - Falls back to code_gen if code omitted or unknown
  - Writes task_type_id, task_complexity_bucket, task_classifier_version='complexity-v1' on insert
- Linter: 3 files checked, 0 hard failures, 0 warnings

## Phase 1 Groups 3–5 (also completed this session)

### Group 3
- migration 016: model_intelligence schema + recommendation_artifacts (passthrough stub)
- migration 017: cost_intelligence.estimate_artifacts
- src/lib/cost-intelligence/types.ts — EstimationFeatures, EstimateResult, etc.
- src/lib/cost-intelligence/deterministic-estimator.ts — pricing lookup + token heuristics
- /api/ingest: writeGroupThreeArtifacts fires on run_started (recommendation + estimate)

### Group 4
- migration 018: runtime_governance schema + budget_periods + budget_reservations + settlement_records
- src/lib/runtime-governance/budget.ts — ensureBudgetPeriod, createReservation, settleReservation
- /api/ingest: reservation created after estimate; budget_period seeded at $9999 on first use

### Group 5
- migration 019: cost_intelligence.evaluation_artifacts
- src/lib/cost-intelligence/evaluation-pipeline.ts — createEvaluationArtifact (idempotent)
- /api/ingest: evaluation + settlement fire on run_completed/run_failed

Linter result: 7 files checked, 0 hard failures, 0 warnings.

## Phase 1 Group 6 (also completed this session)
- migration 020: `public.invariant_report()` Postgres function — bundles all Class C monitoring
  queries into one JSONB blob (telemetry health, coverage gaps, immutability n_tup_upd,
  task distribution, complexity mix, estimation tier breakdown, accuracy overview)
- `src/app/api/monitoring/invariants/route.ts` — calls `supabase.rpc('invariant_report')`
- `src/app/dashboard/invariants/page.tsx` — server component dashboard with PASS/WARN/FAIL
  chips per invariant, pipeline metrics strip, task distribution table, immutability detail,
  complexity + estimation tier breakdown panels
- Sidebar.tsx updated: Invariants nav link with ShieldCheck icon

## Preview attempt
Turbopack panics on Windows directory junctions ("Symlink points out of filesystem root").
The fix is `--no-turbo` in launch.json runtimeArgs. Code is correct — same patterns as all
other dashboard pages. UI verification deferred.

## Apply order (all must be applied in sequence)
013 → 014 → 015 → 016 → 017 → 018 → 019 → 020

## Linter: 8 files checked, 0 hard failures, 0 warnings

## What this session delivered
Complete Phase 1 implementation of the Adaptive Cost Risk Engine:
- 8 Supabase migrations (013–020): cost_intelligence, telemetry, model_intelligence,
  runtime_governance schemas + all Phase 1 artifact tables
- 4 TypeScript library modules: types, estimator, evaluation pipeline, budget helpers
- 3 new API routes: /api/telemetry/ingest, /api/monitoring/invariants, updated /api/ingest
- 1 new dashboard page: /dashboard/invariants

## Post-session fixes (2026-05-19)
- Migrations 012–020 applied directly via Supabase MCP — all confirmed registered
- `invariant_report()` verified live: 51 runs backfilled as code_gen/medium, all immutability counters = 0
- Vercel build fix: `budget.ts` `ensureBudgetPeriod` switched from `insert(onConflict)` to `upsert(ignoreDuplicates)` — `onConflict` is not a valid option on `insert()` in Supabase JS v2
- docs updated: LIVE_SUPABASE_SCHEMA_INVENTORY.md, LESSONS_LEARNED.md
- All changes pushed to main (commit 235cd95)

## PostgREST schema exposure fix (2026-05-19)
- `supabase.schema('x').from('table')` silently returns null for non-public schemas — PostgREST only exposes public by default, and the authenticator role cannot be altered in Supabase.
- Fix: migration 021 adds 4 SECURITY DEFINER functions in public: `get_task_type_id`, `write_run_started_artifacts`, `write_run_completed_artifacts`, `ingest_telemetry_event`.
- Both ingest routes updated to use `supabase.rpc()` exclusively — no more `.schema().from()` calls.

## Code review agent — first end-to-end pipeline test (2026-05-19)
- Run: `CODE_REVIEW_MODEL=claude-sonnet-4-6 python agents/library/code-review-agent/agent.py --commit-sha ab12165 --base-sha 2f7d60a --branch main`
- Result: `APPROVE_WITH_WARNINGS` — 0 critical, 6 warnings, 3 info
- Actual cost: $0.387 | Estimated (p50): $0.033 | Error: 2,248% (first calibration signal)
- Artifacts confirmed: 2 estimates, 1 evaluation, 2 recommendations, 2 reservations, 1 settlement
- Post-review fixes: sentinel UUID guard + typed RPC responses (commit b49ef69)

## Status: COMPLETE AND LIVE
- All migrations 012–021 applied to hdhovyrlnfojtkqbcegh
- main branch at b49ef69
- All artifact tables populated and verified

## Next steps
- Let agents run normally. Watch /dashboard/invariants daily.
- Phase 2 trigger: after 4–6 weeks of accumulated telemetry, declare schema freeze and build calibration pipeline (RFC-006)
