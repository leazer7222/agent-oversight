# Session 2026-06-04 - P2: Rule/Attribute promotion + Gate A Feature Spec

Design session converged on: the graph is the durable artifact; the PRD/Feature Spec is a rendered
projection snapshotted at Gate A; Rule + Attribute promoted as first-class node types in the existing
polymorphic graph (NOT new relational tables). Full design: docs/p2-rule-attribute-gate-a-plan.md.

## Executed (all applied to hdhovyrlnfojtkqbcegh + validated)

- **039** - promote Rule + Attribute to node types. node_type +rule/+attribute, edge_type
  +establishes/+owns, relaxed kind/maps_to_codebase constraints, graph_next_key mints RULE-/ATR-.
  Smoke-validated end-to-end (rule/attr nodes + new edges), cleaned up.
- **042** - backfill: promoted 2 accepted Decision.implies_rules stubs into RULE-0001/RULE-0002
  (establishes + concept citations; idempotent). Forward promotion wired into the dashboard answer
  route (new Decisions mint Rule/Attribute nodes, no stubs).
- **040** - graph_feature_graph: typed fixed-shape transitive traversal (no shared-concept bleed),
  returns the full feature subgraph incl decisions (2 hops, invisible to the old one-hop) and rules
  (3 hops). Fixed a latent bug: graph_feature_detail one-hop never saw Decisions. GET route repointed.
- Renderer src/lib/scoping/render-feature-spec.ts (pure graph->markdown; empty sections "None", no
  orphan prose by construction). ScopingReview gains Rules + Attributes sections + live Feature Spec.
- **041** - graph_gate_a_readiness (H1-H7 hard fails + W4; stronger than scope-readiness). Also added
  output_type 'gate_a_feature_spec' to agent_outputs (later unused - pivoted to a dedicated table).
- **043** - public.gate_a_snapshots (append-only). A human gate artifact does not fit agent_outputs'
  run_id->runs->task_type_id coupling, so the immutable Gate A snapshot lives in its own table.
  gate-a route validates -> renders -> pins manifest of node UUIDs + content hash + provenance +
  PM layer -> supersedes prior -> flips feature to ready. GateAPanel UI.
- **044** - graph_adapter_eng/ux/qa: read-time projections over graph_feature_graph, per-stage slices.
  Downstream agents consume graph projections, never PRD prose.

## First real Gate A
FEAT-0001 (materials catalogue): rules ratified -> graph_gate_a_readiness = ready -> approved Gate A ->
immutable snapshot 8c60561f (content hash f8699b17..., P1, 2 rules, commit-pinned) -> FEAT-0001 = ready.

## Notes / deferred
- Migration 026 (feature_lifecycle spine) NOT applied (chose Option B, self-contained snapshot).
- agent_outputs output_type 'gate_a_feature_spec' is an unused allowed value (harmless leftover).
- AcceptanceCriterion still derived from Rules; promote to a node at the QA stage (Gate C/D).
- Pre-existing local seller-test/seller-v7 files cause 16 tsc errors (gitignored, not in build).

## RESUME FROM HERE
P2 complete (steps 1-5). Next candidates: snapshot-id pinning for adapters; AcceptanceCriterion node
at QA stage; apply feature_lifecycle spine (026) when the FSM drives stages; LLM-assisted
implies_rules classification (set rule kind/normative_force) in the answer route.
