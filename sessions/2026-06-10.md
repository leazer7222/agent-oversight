# Session 2026-06-10

## Focus
PostHog analytics instrumentation audit + integration planning for the ReformAI product
(`ReformAI-Inc/Reform-AI` @ `d768f37`, analyzed via the read-only clone at `.workspace/Reform-AI`).
No changes were made to the product repo — all deliverables are planning docs in this repo's `docs/`.

## Completed
1. **Recovered from a hard tool-deadlock.** An early Bash call did `cd .workspace/Reform-AI`,
   and the Bash tool's CWD persists across calls. The project's PreToolUse hooks use RELATIVE
   paths (`python scripts/hooks/...`), so they resolved into the clone, failed to find the
   script, and python's "file not found" exit code 2 was read by Claude Code as "block tool."
   The `.*` matcher blocked EVERY tool — including the `cd` to undo it. Cleared by a session
   restart (resets Bash CWD).
   - **Permanent fix applied:** all three hook commands in `.claude/settings.json` now use
     `$CLAUDE_PROJECT_DIR/...` absolute paths. Lesson appended to `LESSONS_LEARNED.md`.
2. **Instrumentation audit** -> `docs/reformai-posthog-instrumentation-audit.md`.
   ~110 named events across Platform/Auth, Homeowner, Service Provider, Seller, plus dedicated
   sections for the two hypothesis-critical features (AI Visualizer, Style Tournament). P0/P1/P2
   tiers, snake_case noun_verb names, status-enum reference, and a 12-item Instrumentation Gaps list.
   Key finding: ZERO analytics exist today; the Style Tournament is 100% client-side; Supplier
   persona + "Signature Package" upgrade do not exist yet.
3. **Preliminary integration analysis** (in-conversation) — decisions (region, reverse-proxy,
   identified_only, consent), then a structural readiness scan of the real wiring.
4. **Production implementation plan** -> `docs/reformai-posthog-implementation-plan.md`.
   Engineer-ready: locked decisions D1-D8, Phase 0 (9 tasks w/ real diffs + acceptance), Phase 1
   (P0 events), Phase 2/3 outlines, rollout/risk, and a PR file-change inventory.
   - **Critical correction caught from source:** `distinct_id` must be the **Firebase UID**
     (`user.uid` client / `req.user.firebaseId` server), NOT the DB UUID — the client never holds
     the DB row. DB UUID becomes a person property.
   - **Gap caught:** no `pnpm-workspace.yaml` exists; shared `@reformai/analytics` package needs
     it created (Task 1.0).

## Deliverables (this repo)
- `docs/reformai-posthog-instrumentation-audit.md`
- `docs/reformai-posthog-implementation-plan.md`
- `.claude/settings.json` (hook paths hardened)
- `LESSONS_LEARNED.md` (new lesson: relative hook paths + shell CWD = deadlock)

## Next steps (if resumed)
- Option A: generate branch/PR-ready diffs against the product repo (clone here is read-only).
- Option B: expand Phase 2 full visualizer/style-game event code.
- Option C: draft PostHog dashboard/funnel definitions (insight JSON).

## Notes
- The clone at `.workspace/Reform-AI` is read-only analysis space. Never `cd` into it from a Bash
  call (CWD persists and breaks relative-path hooks). Use absolute paths / Glob-Grep-Read / Explore
  subagents instead.

---

# Session 2 (2026-06-10): Jira Sprint Reporting Agent - design -> build -> register -> activate

## Focus
Designed, built, and registered a Jira Sprint Reporting Agent as a capability inside Agent
Oversight. Live Jira/Confluence data via the Atlassian MCP (site `reform-ai-team.atlassian.net`,
project `RAI`, board 3, Confluence space `RAPD`).

## Completed
1. **Design spec** -> `docs/agent-jira-sprint-reporting.md`. Two-tier model: internal Sprint Review
   Analysis (Confluence) + distilled Management Report (PDF). Count-based (no story points);
   goal-aware health (goal met = GREEN even at low completion %).
2. **Retro template** (`SPRINT RETRO - TEMPLATE`, Confluence page `166297602`) + filled
   `Sprint 1 - Retro` (`166395905`). Key finding: the original retro was a Confluence WHITEBOARD,
   which is NOT API-readable (404). Retro must be a structured page.
3. **Sprint 1 - Review Analysis** (Confluence `166723587`) - full internal doc, brand-styled
   (lozenges + panels; cell background colors get stripped by the HTML->ADF converter).
4. **Management PDF** (`reports/sprint-1-review.html` -> `.pdf`, Edge headless). 4 pages: Sprint 1
   Review, Shipped/Self-Correcting/Risk, Sprint 2 Planning, Sprint 2 Full Scope. Real brand:
   `logo_en.png` + semantic palette from the reconciled `reformai-design-system` skill.
5. **Sprint 2 - Planning** (Confluence `166985730`) - redesigned: readiness gate, scope summary,
   full itemized list (Business Design `RAI-629` broken out), carryover markers.
6. **First Jira WRITE (human-gated):** t-shirt size write-back. "Set Size" column on the planning
   page -> human types sizes -> agent parses published page -> `editJiraIssue` sets
   `customfield_10225`. 12 sizes written + verified. Agent transcribes; never estimates.
7. **Write-boundary found:** the sprint field (`customfield_10020`) only accepts a single id on
   edit; cannot remove issues from sprints (needs Agile board API, not exposed). RAI-201/228/67
   (UAT items done pre-sprint) handled by REPORT-LEVEL EXCLUSION across both sprints.
8. **Registration** (`scripts/register_jira_sprint_agent.js`): definition
   `04c82526-fa49-4241-9bbf-674a0a64108a` (`jira-sprint-reporting-agent`) + instance
   `5544edd7-fe39-4340-9063-f9f71aef85b9` (`reformai.jira-sprint-reporting-agent`), company ReformAI,
   worker, trigger `manual`. Library folder: `agents/library/jira-sprint-reporting-agent/`.
9. **Telemetry runtime** `agent.py` (oversight SDK). `--smoke` mode proven: run
   `51a54fba-1b8e-4747-9d61-f563c12538ce` recorded `status=completed`, `cost_reported=true` (no LLM
   -> $0). Flipped instance to ACTIVE (`set_jira_agent_status.js`), `runtime_implemented=true`.
10. **Atlassian API token** added to `.env.local` (gitignored, verified untracked). Live mode runs
    headless: pulled Sprint 1 raw metrics (29/13/45%) and emitted telemetry (run
    `456ab1af-9513-4c72-b00f-fad02f11e799`).

## Deliverables (this repo)
- `docs/agent-jira-sprint-reporting.md` (spec, Section 14 build notes, Section 12 registration DONE)
- `agents/library/jira-sprint-reporting-agent/` (`agent.json`, `README.md`, `LESSONS.md`, `agent.py`)
- `scripts/register_jira_sprint_agent.js`, `scripts/set_jira_agent_status.js`
- `reports/sprint-1-review.html` / `.pdf` / `logo_en.png`
- `AGENTS.md` (new Jira Team registry entry), `docs/ref-logo-branding-thread.md` (now superseded)

## Pending / next steps
- **Re-verify the live run row** (`456ab1af...`): blocked at session close by a LOCAL DNS failure
  resolving `*.supabase.co` (getaddrinfo failed) - the ingest accepted the telemetry (clean SDK
  posts via Vercel), and the smoke run was confirmed earlier. Re-run the verify node/python snippet
  once DNS recovers.
- Port the full Confluence/PDF authoring into `agent.py` (today the rich authoring is via the
  Atlassian MCP; the runtime covers connect + metrics + telemetry).
- Trigger-on-sprint-close automation; remove RAI-201/228/67 from Sprint 2 in the board UI (Sprint 1
  is a closed sprint, effectively locked).
- The `[HUMAN]` fills on the pages (goal success criteria, strategic recommendations).

## New lessons (candidates for LESSONS_LEARNED)
- Confluence whiteboards are not API-readable (404); cell background colors stripped by HTML->ADF;
  drafts invisible to the API (publish before reading).
- Jira sprint field accepts only a single id on edit; cannot remove from sprints via field edit.
- Query sprint membership by ID not name; exclude sub-tasks; parse big JQL via PowerShell (no jq).
- Node `undici` and Python `httpx` both failed to resolve `*.supabase.co` during a DNS blip while
  Vercel/Jira resolved fine - verification is the only thing affected; ingest persists server-side.
